# changelog.md

> Каждое внедрённое изменение — одна строка, новейшие сверху, с датой.
> Формат: `- ГГГГ-ММ-ДД — Что (кратко) — файлы — Bezug: improvements #NN`

- 2026-07-22 — Напоминания: выбор минут (00,05,…,55) теперь и в папке «В котором часу» — выбор часа спрашивает «✅ Ровно HH:00 / 🔢 Выбрать минуты» (экраны часа/минут — навигация, в диалог входим финальным выбором HH:MM сегодня/завтра); schedule.at_clock_min — features/reminders/handlers.py, features/reminders/schedule.py — Bezug: improvements #09
- 2026-07-22 — Напоминания: на шаге времени тап по пресету часа (напр. 09:00) спрашивает «✅ Ровно 09:00 / 🔢 Выбрать минуты»; минуты — сетка 00,05,…,55; ручной ввод ЧЧ:ММ по-прежнему точный — features/reminders/handlers.py, core/i18n.py — Bezug: improvements #09
- 2026-07-28 — Mini App фиксы/паритет: колёса времени/чисел заменены на нативные input[type=time]/select (старое колесо ломало скролл, т.к. render() пересобирал DOM); убраны две кнопки внизу (свой футер показывался поверх Telegram MainButton — теперь футер только вне Telegram); убран блок быстрых чипсов на экране «Когда?»; добавлен паритет с ботом — правка расписания напоминания (🕐 Время → PATCH /api/reminders/{id}/schedule) + правка текста отдельным экраном вместо prompt — webapp/reminders/index.html, webapp/api.py — Bezug: improvements #24
- 2026-07-28 — Telegram Mini App «Напоминания» (v1): self-contained фронт webapp/reminders/index.html (ванильный JS + Telegram SDK, тема light/dark, 4 языка, экраны список/когда/часы/дни/точное+повтор/календарь/время/текст/карточка/масс-удаление); JSON-API webapp/api.py (initData HMAC-проверка, /api/reminders CRUD+bulk, переиспользует repo/schedule, учёт согласия); раздача статики + /api подключены к FastAPI, /app и menu button открывают приложение — webapp/*, bot_start.py — Bezug: improvements #24
- 2026-07-25 — Напоминания: snooze-кнопки (+10 мин / +1 час / завтра в то же время) теперь только у разовых; у повторяющихся (интервал/ежедн/еженед/ежемес) приходит просто текст без кнопок (они и так сработают снова) — features/reminders/tick.py — Bezug: improvements #10
- 2026-07-25 — Фикс cron-таймаутов: /tick теперь отвечает 200 сразу, тяжёлую работу (напоминания+календарь+бэкап) делает в фоне (asyncio.create_task + guard от наложения) — раньше медленный ICS-фид/пачка напоминаний давали >30 с и cron-job.org отключал джоб за серию неудач; попутно календарный fetch подрезан (таймаут 10 с × 2 попытки ≈ 21 с вместо ~49) — bot_start.py, features/calendar/sync.py — Bezug: improvements #01, #04
- 2026-07-23 — Иконка Админ-панели ⚙️ → 🛠 (чтобы не путать с ⚙️ Настройки), в меню и заголовке панели, все языки — core/i18n.py — —
- 2026-07-23 — Фикс: удаление данных не сбрасывало prime из кэша в памяти — is_prime продолжал возвращать True (доступ «жил»), хотя в списке/БД юзера уже нет; do_delete теперь зовёт admin.forget_prime; контакт в политике → @shyma_6 — core/admin.py, features/settings/handlers.py, core/i18n.py — Bezug: improvements #23
- 2026-07-23 — Datenschutz + Настройки: жёсткое согласие на обработку данных (экран политики на первом /start, без «✅ Согласен» меню закрыто — гейт в /start и dashboard_callback; consent_at в users + миграция); новый модуль ⚙️ Настройки (язык переехал сюда, «📄 Политика» перечитать, «🗑 Удалить все мои данные» — полное стирание контента+профиля во ВСЕХ базах, явными delete без опоры на FK-каскад); политика ru/en/de/uk (данные в Neon+резерв, изоляция, SSL, логи, право на удаление) — features/settings/*, handlers/start_command.py, core/dashboard.py, core/modules.py, DataBase/models.py, DataBase/database.py, bot_start.py, core/i18n.py — Bezug: improvements #23
- 2026-07-23 — Фикс: «зависший» диалог блокировал повторный вход — если открыть окно интервала/повтора, затем /start (глобальный /start не завершает диалог), кнопки-точки-входа не срабатывали, пока не закроешь старое окно; добавлен allow_reentry=True всем ConversationHandler (напоминания/шкаф/календарь/оценки/админка) — features/{reminders,shelves,calendar,grades,admin}/handlers.py — —
- 2026-07-22 — Напоминания, «Точное время / повтор» по образцу папок: время (разовое/ежедн/еженед/ежемес) — полная сетка часов 00–23 → «ровно/минуты» (вместо 5 пресетов); интервал — «каждые [Минуты 5–55 / Часы 1–24 / Дни 1–30]» → сетка N → старт (сейчас / дата+время); убран ручной ввод длительности/времени-пресетов — features/reminders/handlers.py, core/i18n.py — Bezug: improvements #09
- 2026-07-22 — Напоминания: режим выбора для массового удаления — кнопка «🗑 Выбрать / удалить» в списке → чекбоксы ☐/☑️, «выбрать все», «удалить выбранные (N)» и «удалить все» (оба с подтверждением); repo.delete_reminders/delete_all_reminders одним запросом — features/reminders/handlers.py, features/reminders/repo.py, core/i18n.py — Bezug: improvements #15
- 2026-07-22 — Напоминания: папка «Через N дней» переделана как «часы» — сетка 1–30 + неделя/месяц/полгода/год; по умолчанию «✅ То же время (что сейчас)» → сразу текст; опция «🕐 Другое время» → выбор часа → ровно/минуты (00–55); экраны дня/часа/минут — навигация, в диалог входим финальным выбором; убрана «Своё»/шаг времени — features/reminders/handlers.py, features/reminders/schedule.py, core/i18n.py — Bezug: improvements #09
- 2026-07-22 — Напоминания: экран «когда» перестроен на папки — «⏱ Через N часов» (два под-режима: 🕐 конкретный час 00–23 сегодня/завтра и ⏱ через N часов 1–24) и «📅 Через N дней» (1/2/3/неделя/месяц/полгода/год + своё → шаг времени); старые пресеты (завтра утром/вечером/выходные) удалены; интервал/повтор остались в «⚙️ Точное время / повтор» — features/reminders/handlers.py, features/reminders/schedule.py, core/i18n.py — Bezug: improvements #09
- 2026-07-22 — Навигация: «🏠 Домой» добавлена в поток «Точное время / повтор» — на экран выбора типа и на все шаги ввода (дата/время/интервал/старт); «Домой» из шага завершает ConversationHandler (чистит черновик, чтобы не перехватывал следующий ввод) — features/reminders/handlers.py — Bezug: CLAUDE.md §10
- 2026-07-22 — Навигация как правило проекта (CLAUDE.md §10): «‹ Назад» = шаг назад, «🏠 Домой» = старт — в каждом экране; добавлена «🏠 Домой» на оставшиеся вложенные (not-found заглушки шкафа/оценок/напоминаний, админ-экраны модули/prime/ошибки/базы) — CLAUDE.md, features/{shelves,grades,admin,reminders}/handlers.py — —
- 2026-07-22 — Навигация: разделены «‹ Назад» (шаг назад) и «🏠 Домой» (на стартовую); на вложенных экранах (заметка, полка, карточка напоминания, предмет, «за сколько» календаря) рядом с контекстным «назад» добавлена «🏠 Домой»; верхние кнопки модулей «⬅️ Меню» переименованы в «🏠 Домой» — core/i18n.py, features/{shelves,reminders,calendar,grades}/handlers.py — —
- 2026-07-22 — БД: детерминированный фейловер/фейлбек в /tick — на каждом тике проверяется основная; упала → работаем на backup; вернулась (сброс квоты 1-го числа) → копия backup→primary и возврат на primary (раньше выбор по «свежести» застревал на backup навсегда); пока активна primary, backup не трогается кроме суточной копии — DataBase/database.py (set_active, probe_key), features/backup/service.py — Bezug: improvements #21
- 2026-07-22 — Напоминания: кнопки snooze теперь «💤 +10 мин / +1 час / Завтра в то же время»; «Завтра 09:00» убрана из клавиатуры (код tom оставлен для уже отправленных сообщений); «то же время» берёт исходный ЧЧ:ММ напоминания, а не «сейчас» — features/reminders/handlers.py, features/reminders/schedule.py, core/i18n.py — Bezug: improvements #10
- 2026-07-21 — Напоминания: ввод «когда» разбит на отдельные шаги — сначала дата (Сегодня/Завтра/своя), потом время (пресеты 09–21 или своё), у интервала: длительность → старт (сейчас/дата→время); на каждом шаге кнопки-пресеты + ручной ввод, ничего не проскакивает, есть «назад к дате»; убран переключатель формата (dt/time/interval); правка времени идёт теми же шагами — features/reminders/handlers.py, features/reminders/schedule.py, core/i18n.py — Bezug: improvements #09
- 2026-07-21 — Фикс: /start строил меню без uid → показывал common-вид всем (админ/prime не видели свои кнопки до первого перехода «в модуль и назад»); теперь меню сразу по уровню пользователя — handlers/start_command.py — Bezug: improvements #22
- 2026-07-20 — Prime-доступ (уровни common<prime<admin): модули помечаются в панели «👥 всем»/«⭐ только prime» (uid в build_dashboard_markup + can_see на роутере), обычный юзер просит доступ кнопкой «⭐ Запросить доступ» в меню или /prime → заявка в очередь (id/username/время) с уведомлением админа; в панели экран Prime — список участников (снять), очередь (одобрить/отклонить с DM юзеру), добавить по ID (диалог); prime-членство в таблице prime_users (+prime_requests, обе в зеркале-резерве); /whoami говорит уровень (admin/prime/common) — DataBase/models.py, DataBase/database.py, core/admin.py, core/dashboard.py, features/admin/handlers.py, core/i18n.py — Bezug: improvements #22
- 2026-07-20 — Горячий резерв БД: вторая база DATABASE_URL_2, бот при старте сам выбирает активную (живая с самой свежей меткой db_heartbeat, при равенстве — основная), авто-фейловер на резерв и авто-возврат после выравнивания; ежедневное зеркалирование активной базы в резерв через /tick (гейт по дате) + метка свежести каждый тик; repo переведены с `is None` на прокси async_session (смена активной БД видна без переимпорта); админ-панель показывает статус баз (🟢активна/⚪резерв/🔴недоступна + занято/юзеры), переключения нет — только просмотр — DataBase/database.py, features/backup/*, features/admin/handlers.py, features/*/repo.py, bot_start.py, core/i18n.py — Bezug: improvements #21
- 2026-07-20 — Админ-панель (только ADMIN_ID из окружения): ⚙️ модуль виден лишь админу, кнопки вкл/выкл каждого модуля для всех юзеров (флаг в app_settings + кэш в памяти, при мёртвой БД действует до рестарта), журнал последних 20 ошибок; обычный юзер у выключенного модуля видит 🔧 и «техработы», админ — настоящий текст; on_error шлёт админу traceback в личку (антифлуд 1/мин), юзеру — нейтральное «техработы»; /whoami показывает свой Telegram ID; проверка прав на роутере (callback подделываем) — DataBase/models.py (таблица app_settings), DataBase/database.py, core/admin.py, core/dashboard.py, core/registry.py, features/admin/*, bot_start.py, core/i18n.py — Bezug: —
- 2026-07-20 — Напоминания, шаг «когда»: переключатель формата ввода [📅 Дата][🕐 Время][⏱ Интервал] прямо на шаге (активный помечен точкой, тип повтора не теряется при совпадении формата) + у интервала спрашивается момент старта («Прямо сейчас» / своя дата); парсеры parse_datetime/parse_time/parse_interval вынесены из parse_when — features/reminders/handlers.py, features/reminders/schedule.py, core/i18n.py — Bezug: improvements #09, #11
- 2026-07-20 — Neon-квота: NullPool для asyncpg (соединение закрывается после запроса, compute засыпает вместо круглосуточной работы); init_db в lifespan обёрнут в try/except — недоступная БД больше не роняет старт сервиса — DataBase/database.py, bot_start.py — Bezug: improvements #17, #18
- 2026-07-03 — Оценки: своя формула для шкалы 1–6 (каждая SA ×2, устная ×1: SA 4 + M 5 → 4.33; типов только SA и Mündlich, кнопка KA скрыта) — features/grades/logic.py, handlers.py, core/i18n.py — —
- 2026-07-03 — Оценки: вторая шкала «1–6 Noten» (выбор шкалы при создании предмета, целые 1–6, границы ввода по шкале, Gesamtschnitt раздельно по шкалам; поле grade_subjects.scale + миграция) — DataBase/models.py, DataBase/database.py, features/grades/*, core/i18n.py — —
- 2026-07-03 — Глобальный error handler PTB: traceback в лог Render + короткое сообщение об ошибке в чат (кнопки больше не «умирают» молча) — bot_start.py — —
- 2026-07-03 — Кнопка языка: «🇺🇦 Українська (з приколами)» → «🇺🇦 Українська» (по просьбе пользователя) — core/i18n.py — —
- 2026-07-03 — Четвёртый язык: 🇺🇦 украинский в шутливом стиле (словарь _UK на все ~160 ключей, вливается в _T; uk в LANGS/norm_lang/переключателе) — core/i18n.py, core/dashboard.py — —
- 2026-07-03 — Новый модуль «🎓 Оценки» (Notenrechner): предметы создать/переименовать/удалить, оценки SA/KA/Mündlich (баллы 0–15) добавить/удалить, средний по предмету по формуле из example/Noten.xlsx (маленькие = (2·KA+Mündl)/3, итог = (SA+маленькие)/2) + Gesamtschnitt; таблицы grade_subjects/grade_entries; ru/en/de — DataBase/models.py, features/grades/*, core/i18n.py, bot_start.py — —
- 2026-07-02 — Мультиязычность ru/en/de: словарь core/i18n.py (~100 ключей), поле users.language (+миграция), авто-детект из language_code Telegram, пункт меню «🌐 Язык»; переведены все кнопки/тексты (меню, Шкаф, Напоминания, Календарь, /start /stop /support), ошибки ввода несут i18n-ключи (ParseError/FeedError), фоновые /tick-отправки на языке владельца — core/i18n.py, core/dashboard.py, core/registry.py, core/modules.py, DataBase/models.py, DataBase/database.py, handlers/*, features/shelves|reminders|calendar/* — —
- 2026-07-02 — Календарь: свой ввод времени предупреждения с единицами (45m/2h/1d и м/ч/д, не только минуты) — features/calendar/sync.py, handlers.py — Bezug: business_plan Schritt 5
- 2026-07-02 — Календарь: настраиваемое время предупреждения (кнопка «⏱ За сколько»: пресеты 10 мин–1 день + свой ввод в минутах, 5–10080; поле lead_minutes у подписки + мини-миграция ALTER TABLE в init_db) — DataBase/models.py, DataBase/database.py, features/calendar/repo.py, tick.py, sync.py, handlers.py — Bezug: business_plan Schritt 5
- 2026-07-02 — Фикс билда Render (ResolutionImpossible): tzdata 2025.2 → 2026.2, т.к. icalendar 7.2 требует >=2025.3 — requirements.txt — Bezug: business_plan Schritt 5
- 2026-07-02 — Шаг 5 (чтение): модуль «Календарь» — подписка на публичный ICS-фид (webcal/https), таблицы CalendarFeed/CalendarEvent, синк в /tick (кулдаун 30 мин, retry+backoff, URL не в логах/UI), напоминание за 30 мин до события (весь-день — утром), сдвиг/отмена события учитываются; +icalendar — DataBase/models.py, features/calendar/*, core/modules.py, bot_start.py, requirements.txt — Bezug: business_plan Schritt 5, improvements #04 #05
- 2026-07-01 — Напоминания: редактирование даты/времени (кнопка «🕐 Время») + повтор «Ежемесячно» (кламп дня, каждое 31-е) — features/reminders/schedule.py, repo.py, handlers.py — Bezug: improvements #11 #12
- 2026-07-01 — Напоминания: кнопка «⬅️ Отмена» на шагах ввода (можно отказаться от выбранной опции, не только /cancel) — features/reminders/handlers.py — Bezug: improvements #09
- 2026-07-01 — Напоминания UX инкремент 1: пресеты времени (Через 1/3ч, Сегодня вечером, Завтра утром/вечером, Выходные) + Snooze-кнопки под пришедшим напоминанием (+10м/+1ч/Завтра) — features/reminders/schedule.py, repo.py, handlers.py, tick.py — Bezug: improvements #09 #10
- 2026-07-01 — Фикс «мёртвых кнопок» после засыпания Render: answer() сделан некритичным, добавлены edit_safely/answer_safely (глотают устаревший callback и «message not modified»); держать инстанс тёплым внешним cron на /tick — core/dashboard.py, core/modules.py, features/shelves/handlers.py, features/reminders/handlers.py — Bezug: improvements #08
- 2026-06-30 — Шаг 4: модуль «Напоминания» — таблица Reminder, повторы once/daily/weekly/interval (TZ Europe/Berlin→UTC), редактируемый текст, пауза; эндпоинт /tick (секрет X-Tick-Key) + process_due для внешнего cron (5 мин); +tzdata — DataBase/models.py, features/reminders/*, core/modules.py, bot_start.py, requirements.txt — Bezug: business_plan Schritt 4, improvements #01 #02
- 2026-06-30 — Шаг 3: модуль «Шкаф» — таблицы Shelf/Note + полный CRUD полок/заметок (диалог ввода), изоляция по владельцу, лимиты длины; ssl только для postgres-URL — DataBase/models.py, DataBase/database.py, features/shelves/*, core/modules.py, bot_start.py — Bezug: business_plan Schritt 3, improvements #07
- 2026-06-30 — Шаг 2: скелет дашборда — реестр модулей + инлайн-меню на /start + callback-роутер; 3 модуля-заглушки (Шкаф/Напоминания/Календарь) — core/registry.py, core/dashboard.py, core/modules.py, core/__init__.py, handlers/start_command.py, bot_start.py — Bezug: business_plan Schritt 2
- 2026-06-30 — Фикс БД-URL: нормализация строки Neon под asyncpg (драйвер + убраны sslmode/channel_binding, ssl=True); подключение проверено вживую — DataBase/database.py — —
- 2026-06-30 — Шаг 1: БД-слой (SQLAlchemy async + Neon) + модель User + авто-регистрация юзера на /start (мягкая деградация без DATABASE_URL) — DataBase/models.py, DataBase/database.py, DataBase/__init__.py, bot_start.py, handlers/start_command.py, requirements.txt — Bezug: business_plan Schritt 1
- 2026-06-23 — Стек уточнён по факту: python-telegram-bot 22.3 (не aiogram), БД Neon (Postgres), старт разработки с БД-схемы + регистрации юзера на /start — business_plan.md — —
- 2026-06-23 — В .gitignore добавлены reminders.json (локальные данные) и price_checker.py (проектно-чужой файл) — .gitignore — —
- 2026-06-23 — Репозиторий очищен: добавлен .gitignore, venv/ и __pycache__ убраны из отслеживания (файлы на диске сохранены) — .gitignore, changelog.md — —
- 2026-06-23 — БД-решение закрыто: хранилища в боте нет → заводим Neon (Postgres) с нуля — business_plan.md — —
- 2026-06-23 — Учтено: webhook уже в проде (переиспользуем); /tick-пинг оставлен только для таймера напоминаний; БД — переиспользовать существующую или Neon — business_plan.md — —
- 2026-06-23 — Решения: БД Neon (Render free эфемерен), напоминания через внешний cron+/tick (free-инстанс засыпает), старт с модуля «Шкаф», связь шкаф↔напоминания отложена — business_plan.md, improvements.md — —
- 2026-06-23 — План v2: запись в календарь через .ics с подтверждением юзера + редактируемый текст напоминаний — business_plan.md, improvements.md — —
- 2026-06-23 — Финализирован план: календарь read-only через URL-подписку (убраны Apple ID/CalDAV/шифрование секрета) — business_plan.md, improvements.md — —
- 2026-06-23 — Проектная документация создана (business_plan, improvements, changelog, CLAUDE.md) — — —
